type product {
  id: ID!
  name: String!
  category: String!
  price: Float!
}
type supplier {
  id: ID!
  name: String!
  city: String!
  products: [product]
}
type Query {
  products: [product]
  suppliers: [supplier]
}
